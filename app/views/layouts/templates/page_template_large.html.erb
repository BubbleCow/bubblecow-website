<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render 'shared/layout/head' %>
  </head>

  <body>

    <!-- Google tag manager -->
    <%= render 'shared/layout/google_tag_manager' %>

    <!-- navigation -->
    <%= render 'shared/layout/navigation/navbar' %>

    <!-- Notices -->
    <%= render 'shared/layout/notices' %>

    <!-- main -->
    <main>
      <div class="container">
        <div class="row justify-content-center">
          <div class="col-12 col-sm-10 col-md-10 col-lg-10">
            <%= yield %>
          </div>
        </div>
      </div>
    </main>


    <!-- Footer -->
    <%= render 'shared/layout/footer' %>

  </body>

</html>


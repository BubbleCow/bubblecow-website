<h1>Testimonials</h1>


  <% @testimonials.each do |testimonial| %>
      <div class="card mb-3">
        <div class="card-body">
          <p class="card-text"><%= testimonial.testimonial_text %></p>
          <h6 class="card-subtitle"><%= testimonial.writer.titleize %></h6>
          <% if current_user.try(:admin?) %>
            [<%= link_to "edit", edit_testimonial_path(testimonial), class: "card-link" %> | 
            <%= link_to "delete", testimonial_path(testimonial), method: :delete, data: { confirm: "Are you sure you want to kill this bad boy?", class: "card-link"} %>]
          <% end %>
        </div>
      </div>
  <% end %>
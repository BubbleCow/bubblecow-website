<section>
    <div class="box mt-2">
        
        <h2 class="mb-3">Add a service...</h2>

        <p>To get started you need to select an editorial service and upload your unedited manuscript.</p>
        <p>Please note we accept most common files formats, though we will be editing in Word format.</p>
        <p>We don't accept PDF format.</p>

        <div class="form">

            <%= form_with(model: [@book, @order], local: true) do |form| %>

                <div class="form-group mb-3">
                    <%= form.select :product_id, options_from_collection_for_select(Product.all, :id, :name), {prompt: "Select a product"}, {class: 'form-select'} %>
                </div>

                <div class="form-group">
                    <p>Attached your book...</p>
                    <%= form.file_field :initial_unedited_manuscript, class: 'form-select' %>
                </div>
                
                <div class="d-grid gap-2">
                    <%= form.submit "Submit your book", class: "button btn btn-success" %>
                </div>

            <% end %>

        </div>

    </div>
</section>